## feature/debugger

* Created a console debugger `luadebug.lua` for debugging of external and
  builtin Lua modules;
* New Lua global function created `tarantool_builtin_module` which allows
  to see sources of builtin modules in any external debugger;
