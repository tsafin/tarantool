[default]
core = app
description = tarantool multiengine tap tests
is_parallel = True
use_unix_sockets_iproto = True
config = engine.cfg
lua_libs = lib/tester.lua ../box/lua/utils.lua ../box/lua/push.lua
fragile = {
    "retries": 1,
    "tests": {
    }
  }
